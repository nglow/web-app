spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:test
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: sql
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace